# atui
_This app is only available in Japanese. The following is all written in Japanese._

Atui is the browser assistant made by japanese high school student.

## About Us
Atuiは日本の高校生が一から開発したアシスタントです。
これは現在サポートされている、すべての端末・OSで動くことを目標に作られました。
<sub>動作確認済みOS：Windows10, Windows11, Linux(Ubuntu), Android16∼13, iOS 26∼15</sub>

## What Can I Do?
AtuiではAIなどの"なんでもできる"プログラムをまったく使用しないため、利用できる機能はAIに比べたら限られています。
しかし、Atuiの一部の機能はAPIからデータを取得して動作していますので軽量かつ必要なデータのみを表示できます。（無駄な情報がない）

以下はAtuiで利用できる機能の一覧です：

  > `天気`     日本の天気を取得します。都道府県単位はもちろん、地域ごとの天気を調べることができます。livedoor提供の"地域コード"に対応しています。
  >   
  > `電卓`     新規ウィンドウで簡単な計算ができる電卓を表示します。関数計算は現時点では不可能です。
  >   
  > `ＱＲ`     ユーザーが入力したURLまたはテキストをQRコードに変更します。
  >    
  > `勉強`     勉強をサポートするウェブアプリに誘導します。
  >   
  > ` 暇 `     暇つぶし動画。海外では有名なあの動画が開きます。
  >   
  > `しりとり` ボットとしりとりができます。しりとりはローカルで動きますが、まだまだボットが知っている日本語が少ないため、ユーザーが強制的に敗北されるケースが確認されています。ご了承ください。
  >   
  > `郵便番号` 郵便番号から地域に変換します。_（例：1008959 → 東京都千代田区霞が関）_ また、その逆にも対応しております。(remaked版でハイフンありを許容)
  >   
  > `消火` `着火`  atuiを消火、着火できます。消火中はまともにしゃべりません。 (remaked版実装)
  >
  > `じゃんけん`  atuiとじゃんけんできます。(remaked版より実装)

## How To Use
![usage image](https://github.com/user-attachments/assets/e836291a-95f5-4b54-acf7-06586ecd8e00)

上記の利用できる機能(以下はコマンドと呼ぶ)は画面下部に表示されるテキストボックスに入力して利用できます。
また入力するコマンドは"天気"や"QR"など、画像のように文章になっていなくても大丈夫です。

## Development Environment
リメイク時にコード方針や使用ツールを大幅に見直し、より安全で高度な開発を可能にしました。
- ✅ `vite`によるコードバンドル
- ✅ `TypeScript`を用いた型安全なコード設計
- ✅ HTMLとAtui本体が直接結びつかない疎結合型プログラム
- ✅ スマホとデスクトップを単一ファイルに統一

## Build / Development Server
1. リポジトリをクローンします。
2. `npm i`でパッケージをインストールします。
3. `npm run dev`で開発用サーバーが`http://localhost:5173`で自動的に起動します。
5. `npm run build`で`dist/`下にバンドルされたatuiウェブアプリが出力されます。